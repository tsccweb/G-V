# Example backend environment variables for Render
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"
JWT_SECRET="replace_with_a_strong_secret"
# Optional: used by your script and Prisma if needed
NODE_ENV=production
